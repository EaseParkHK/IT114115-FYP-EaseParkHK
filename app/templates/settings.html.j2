{% extends "base.html.j2" %}
{% import 'bootstrap/wtf.html' as wtf %}
{% block app_content %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Settings Page</h1>
        {% if current_user.is_authenticated %}
            <!-- User Details Update Form -->
            <form action="{{ url_for('settings') }}" method="post">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label for="username" class="form-label">Username</label>
                        <input type="text"
                               class="form-control"
                               id="username"
                               name="username"
                               value="{{ current_user.username }}"
                               placeholder="Enter your username">
                    </div>
                    <div class="col-md-6">
                        <label for="email" class="form-label">Email</label>
                        <input type="email"
                               class="form-control"
                               id="email"
                               name="email"
                               value="{{ current_user.email }}"
                               placeholder="Enter your email">
                    </div>
                </div>
                <button type="submit" class="btn btn-primary w-100">Update</button>
            </form>
        {% else %}
            <div class="alert alert-warning text-center mt-4" role="alert">Please log in to update your settings.</div>
            <div class="text-center">
                <a href="{{ url_for('login') }}" class="btn btn-primary btn-lg mt-3">Log In</a>
            </div>
        {% endif %}
    </div>
     <!-- Chatbox Button -->

<script>
    function toggleChatbox() {
        const chatboxPopup = document.getElementById('chatbox-popup');
        chatboxPopup.style.display = chatboxPopup.style.display === 'none' ? 'block' : 'none';
    }
</script>
{% endblock %}
